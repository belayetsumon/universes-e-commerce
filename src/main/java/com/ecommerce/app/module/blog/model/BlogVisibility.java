package com.ecommerce.app.module.blog.model;

public enum BlogVisibility {
    PUBLIC,
    CUSTOMER_ONLY,
    VENDOR_ONLY,
    MEMBERS_ONLY,
    PASSWORD_PROTECTED,
    COUNTRY_RESTRICTED,
    LANGUAGE_RESTRICTED,
    DEVICE_RESTRICTED
}
